# Feature Milestone 3 Iterations: GLearning

**Metric:** Feature Milestone 3 checklist score across the remaining post-FM2 launch expansion areas: deeper mobile polish, screenshots/demo, non-Wuwa connector-ready architecture, share/profile/account/cloud path, and voice/pronunciation.
**Stop criterion:** Continue one attributed milestone loop at a time until the Oracle says no further high-value prompt remains for this release lane, or the remaining work requires external assets/accounts/permissions that cannot be completed safely in-repo. Each landed loop must pass `npm run build`; route/API/deploy changes require local/prod smoke.
**Scope:** `src/App.tsx`, `src/styles.css`, `src/gameData.ts`, `functions/api/*`, `README*`, support docs/assets/scripts if added, and prompt-export scoreboards.
**Baseline date:** 2026-05-04
**Baseline release:** `18f2ce3` (`Record language help production smoke`) deployed to `https://glearning.pages.dev`.

## Checklist model

| Area | FM3+ target | Baseline |
|---|---|---|
| Mobile polish | Reader and `/saved` ergonomics explicitly improved for phone/tablet widths and documented/smoked | Responsive CSS exists; deeper polish pending |
| Screenshots/demo | Repo includes reproducible demo/screenshot capture path or visual demo docs | Smoke docs only |
| Non-Wuwa connector path | Connector-ready architecture or one safely gated non-Wuwa source path without fake live claims | Sample readers only beyond Wuwa |
| Share/profile/account/cloud | Safe local MVP or honest implementation path for share/profile/account/cloud sync | TSV export + local saved/review only |
| Voice/pronunciation | Privacy-gated local practice prototype or explicit technical plan | Playback only |

## Runs

| # | Change | Score | Build / Smoke | Notes |
|---|---|---:|---|---|
| baseline | Feature Milestone 2 production release | 0 / 5 FM3+ areas upgraded; FM2 remains released | build pass (`npm run build`; Vite 552ms; CSS 59.93 kB gzip 11.27 kB; JS 293.76 kB gzip 92.54 kB) | `/saved` and richer deterministic language help are live. Remaining FM3+ areas should be tackled one attributed MVP at a time. |
| 1 | Mobile reader/saved ergonomics MVP | 1 / 5 FM3+ areas upgraded | `npm run build` pass (`EXIT:0`; Vite 606ms; CSS 62.25 kB gzip 11.72 kB; JS 293.76 kB gzip 92.54 kB). Local Pages (`npx wrangler pages dev dist --compatibility-date=2026-05-02`) parsed 3 redirects/1 header; Node fetch smoke passed: `/`, `/saved`, `/saved/`, `/games/wuwa`, `/games/cyberpunk`, `/audio/manifest.json` all 200; `/api/main-quests` 200 with 56 quests; `/api/quest?...zhUrl=auto` 200 with paired 284, audioCount 220, lines 314, hasLocalAudio true. Headless 390x844 viewport smoke passed: Wuwa reader order 1 before left/right rails 2/3, bodyScrollWidth 390/no overflow, dock height 159 with reader padding-bottom 274 and contentClearsDock true; Cyberpunk sample route no overflow and shows `Audio: No playable clips yet`; `/saved` no overflow with 44px filter controls. | CSS-only responsive change in `src/styles.css`; docs updated. No `src/App.tsx`, APIs, storage schemas, connector code, language-help logic, dependencies, redirects, screenshots/demo, account/cloud/share, or voice/pronunciation changes. Orchestrator reverified build (`npm run build`; Vite 627ms) and local Pages route/API smoke with the same endpoint counts. |
| 2 | Launch Demo Route MVP | 2 / 5 FM3+ areas upgraded | `npm run build` pass (`EXIT:0`; Vite 649ms; CSS 65.75 kB gzip 12.17 kB; JS 298.33 kB gzip 93.51 kB). Local Pages (`npx wrangler pages dev dist --compatibility-date=2026-05-02 --port 8789`) parsed 5 redirects/1 header with no redirect warning; Node fetch smoke passed: `/`, `/demo`, `/demo/`, `/saved`, `/saved/`, `/games/wuwa`, `/games/cyberpunk` all 200 HTML; `/audio/manifest.json` 200 with 258 entries; `/api/main-quests` 200 with 56 quests; `/api/quest?...Utterance...&zhUrl=auto` 200 with paired 284, audioCount 220, lines 314, hasLocalAudio true. Headless `agent-browser` 390x844 `/demo` smoke passed: bodyScrollWidth 390, documentScrollWidth 390, no horizontal overflow; copy checks true for Wuwa-only live connector, non-Wuwa sample-reader honesty, browser-local saved/review, TSV export, and deferred account/cloud/share/voice claims; after clearing storage and reloading `/demo`, only existing theme/density keys were written (`glearning-density`, `glearning-palette-wuwa`) and `/api` network request log was empty. | Added static `/demo` route, Home CTA, scoped demo CSS, explicit `/demo` + `/demo/` Pages fallbacks, and README smoke/honesty docs. Demo render guards skip quest/API fetches and saved/review writes; no APIs/functions, Wuwa connector behavior, language-help logic, storage schemas, dependencies, account/cloud/share/voice features, or sample-game live claims changed. Orchestrator reverified build (`npm run build`; Vite 610ms) and local Pages route/API smoke including `/demo` and `/demo/`; the attempted static-HTML copy assertion was discarded because React SPA copy renders client-side, while browser smoke evidence remains valid. |
| 3 | Non-Wuwa Connector-Ready Architecture MVP | 3 / 5 FM3+ areas upgraded | `npm run build` pass (`EXIT:0`; Vite 532ms; CSS 65.75 kB gzip 12.17 kB; JS 300.54 kB gzip 93.83 kB). Local Pages (`npx wrangler pages dev dist --compatibility-date=2026-05-02 --port 8789`) parsed 5 redirects/1 header; Node fetch smoke passed: `/`, `/demo`, `/demo/`, `/saved`, `/saved/`, `/games/wuwa`, `/games/cyberpunk` all 200 HTML; `/audio/manifest.json` 200 with 258 entries; `/api/main-quests` 200 with 56 quests; `/api/quest?...Utterance...&zhUrl=auto` 200 with title `Utterance of Marvels: I`, zhTitle `万象新声·上`, paired 284, audioCount 220, lines 314, hasLocalAudio true. `agent-browser` network instrumentation: `/games/cyberpunk` after 8s had readerTitle `The Streetkid`, `8 paired · 0 audio visible · 0 unmatched`, sample-set context, and `apiResources: []`; `/games/wuwa` loaded live data after wait with `/api/main-quests`, `/api/quest?...Utterance...`, paired 284, audioCount 220, and no error. | Added typed connector metadata/capability helpers in `src/gameData.ts` and used them in `src/App.tsx` to gate live quest/catalog fetches; Wuwa remains the only live connector, non-Wuwa routes still render `createSampleQuest(activeGame)` and make no live API calls on initial sample render. README files document connector-ready architecture honestly. No Cloudflare functions, API contracts, saved/review storage, language-help logic, audio behavior, dependencies, redirects, account/cloud/share/voice features, or sample-game live claims changed. Orchestrator reverified build (`npm run build`; Vite 665ms) and local Pages route/API smoke with `/demo`, `/games/wuwa`, `/games/cyberpunk`, `/api/main-quests`, and `/api/quest` counts unchanged. |
| 4 | Local Data Snapshot Export MVP | 4 / 5 FM3+ areas upgraded | `npm run build` pass (`EXIT:0`; Vite 630ms; CSS 67.07 kB gzip 12.33 kB; JS 303.60 kB gzip 94.81 kB). Local Pages (`npx wrangler pages dev dist --compatibility-date=2026-05-02 --port 8789`) parsed 5 redirects/1 header; Node fetch smoke passed: `/`, `/demo`, `/demo/`, `/saved`, `/saved/`, `/games/wuwa`, `/games/cyberpunk` all 200 HTML; `/audio/manifest.json` 200 with 258 entries; `/api/main-quests` 200 with 56 quests; `/api/quest?...Utterance...&zhUrl=auto` 200 with title `Utterance of Marvels: I`, zhTitle `万象新声·上`, paired 284, audioCount 220, lines 314, hasLocalAudio true. `agent-browser` `/saved` export smoke after seeded localStorage captured JSON with `app: GLearning`, `version: 1`, claims `account/cloudSync/publicProfile/shareCards/restoreImport: false`, counts savedItems 2, savedLines 1, savedTerms 1, reviewItems 1, studyStates 1, studyLines 2, preferences 2; `fetchCalls: []`, `apiResources: []`, `newKeys: []`, success notice visible, and local-only boundary copy present. | Added `/saved` Local data snapshot panel/button, versioned JSON snapshot helpers, safe `glearning-study-v1:*` scan, sanitized saves/reviews, lightweight `glearning-density`/`glearning-palette-*` preferences, local success/error notice UI, scoped mobile-safe styles, and README smoke docs. Product boundary remains local export only: no account, no cloud sync, no public profile, no share cards, and no restore/import yet. No storage schema, save/review grading, study persistence behavior, routes, APIs/functions, connector behavior, language-help logic, audio behavior, dependencies, redirects, voice/pronunciation, import/login/cloud/public sharing/profile publishing changes. Orchestrator reverified build (`npm run build`; Vite 585ms) and local Pages route/API smoke with endpoint counts unchanged. |
| 5 | Mic-free Shadow Practice MVP | 5 / 5 FM3+ areas upgraded | `npm run build` pass (Vite 683ms; CSS 71.08 kB gzip 12.90 kB; JS 308.67 kB gzip 96.35 kB). Local Pages (`npx wrangler pages dev dist --compatibility-date=2026-05-02 --port 8791`; port 8789 was already in use) parsed 5 redirects/1 header; Node fetch smoke passed: `/`, `/demo`, `/demo/`, `/saved`, `/saved/`, `/games/wuwa`, `/games/cyberpunk` all 200 HTML; `/audio/manifest.json` 200; `/api/main-quests` 200 with 56 quests; `/api/quest?...Utterance...&zhUrl=auto` 200 with title `Utterance of Marvels: I`, zhTitle `万象新声·上`, paired 284, audioCount 220, lines 314, hasLocalAudio true. Forbidden API grep over `src functions` for `getUserMedia|MediaRecorder|SpeechRecognition|webkitSpeechRecognition` returned no matches. `agent-browser` 390x844 smoke: `/games/wuwa` Voice tab safety copy checks true, `Shadow practice` selected a source-audio line, reused existing source player, produced chunks `Looks like drowning.` / `Strange.`, incremented `Session reps` to 1, created `newKeys: []`, and had no voice/shadow/rep storage key; `/games/cyberpunk` Voice tab safety copy checks true, selected sample line showed no source clip plus no generated audio/scoring, no source player, 3 chunks; reload reset sample `Session reps` from 1 to 0. | Added a `Voice` right-tab, dialogue-card `Shadow practice` action, `VoicePracticePanel`, deterministic `buildShadowPracticeChunks`, selected voice line state, and session-only repetition counts in `src/App.tsx`; reused existing `VoicePlayer`/`markLinePlayed` when source audio exists. Updated `src/styles.css` for five responsive right tabs and scoped shadow-practice/mobile styles. Updated README files with mic-free voice boundaries and forbidden API checks. No microphone, recording, upload, speech recognition, scoring/rating/accuracy claim, TTS/generated audio, new storage schema/key, saved/review behavior, language-help rules, connector metadata, Cloudflare Functions, audio manifest behavior, dependencies, routes, redirects, account/cloud/share/import changes. Orchestrator reverified build (`npm run build`; Vite 655ms), local Pages route/API smoke with endpoint counts unchanged, and forbidden API grep with no matches. |

## Closeout

FM3 automatic milestone loop closed at **5 / 5 FM3+ areas upgraded** after Oracle stop judgment. No next safe in-repo FM3 milestone remains: further work now depends on real external connectors, account/cloud/profile/public sharing, restore/import, recording/pronunciation scoring, screenshots/video assets, or explicit rights/privacy/legal review.
